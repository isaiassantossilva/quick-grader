/*
import 'dart:io';

import 'package:flutter_test/flutter_test.dart';
import 'package:integration_test/integration_test.dart';
import 'package:quick_grader/service/answer_sheet_generator_service.dart';

void main() {
  IntegrationTestWidgetsFlutterBinding.ensureInitialized();

  test('Deve criar um png com uma folha de respostar', () async {
    final answerSheetGeneratorService = AnswerSheetGeneratorService();

    /*
    final answerSheet = await answerSheetGeneratorService.generateAnswerSheet(
      10,
      5,
    );

    expect(answerSheet, isNotNull);

    // Salvar arquivo para verificação visual (opcional)
    final directory = "../images";
    final filePath = '$directory/test_answer_sheet.png';
    final file = File(filePath);
    await file.writeAsBytes(answerSheet!);
    */
  });
}
*/
